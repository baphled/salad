<%= link_to 'New Resource information', new_parking_path %>

<% semantic_form_for :parking,  :url => { :action => "tickets" }  do |form| %>
    <%= collection_select :resource, :id, Resource.all, :id, :name, options = {:prompt => "Select a resource"} %><br />
    <%= form.text_field :tag %>
  <%= render :partial => "/common/save_button" %>
<% end %>