
<h3>Steps</h3>
<%= render 'step_search' %>
<%= render :partial => 'common/cloud_tag', :locals => {:tags => @tags}%>

<%= render :partial => '/common/sortable_list',
  :locals => {:models => @steps,
  :item_name => 'step',
  :method => :formatted,
  :method_arg => @steps,
  :assoc => nil,
  :order => false}%>